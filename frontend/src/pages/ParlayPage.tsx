import { CircleCheckBig, Layers3, ShieldAlert } from "lucide-react";
import { ErrorPage, LoadingPage, PageHeader, Panel, StatusBadge } from "../components/Primitives";
import { useDashboard } from "../hooks/useDashboard";
import { american, currency, percent } from "../utils/format";

export function ParlayPage() { const query = useDashboard(); if (query.isLoading) return <LoadingPage />; if (query.isError || !query.data) return <ErrorPage message={query.error?.message || "Parlay state unavailable"} retry={() => void query.refetch()} />; const parlay = query.data.recommendations.find((item) => item.kind === "parlay"); return <div className="page-stack"><PageHeader eyebrow="Optional risk sleeve" title="Parlay of the day" description="At most one qualified 2–3 leg cross-event parlay. Zero is a valid and expected outcome." />{parlay ? <Panel title="Qualified parlay" eyebrow="Verified executable quote"><div className="parlay-page-grid"><div><StatusBadge tone="positive">Qualified</StatusBadge><h2>{parlay.legs.length}-leg portfolio sleeve</h2>{parlay.legs.map((leg, index) => <div className="parlay-leg" key={leg.event_id}><span>{index + 1}</span><div><strong>{leg.selection}</strong><small>{leg.sportsbook} · {american(leg.odds)} · EV {percent(leg.ev_per_unit)}</small></div></div>)}</div><dl className="decision-metrics"><div><dt>Offered odds</dt><dd>{american(parlay.odds)}</dd></div><div><dt>Joint fair probability</dt><dd>{percent(parlay.fair_probability)}</dd></div><div><dt>Joint EV</dt><dd>{percent(parlay.ev_per_unit)}</dd></div><div><dt>Stake</dt><dd>{currency(parlay.stake)}</dd></div><div><dt>Bankroll</dt><dd>{percent(parlay.bankroll_fraction)}</dd></div><div><dt>Correlation</dt><dd>Explicitly checked</dd></div></dl></div></Panel> : <Panel><div className="parlay-pass-hero"><div className="pass-icon large"><CircleCheckBig /></div><StatusBadge tone="neutral">PASS</StatusBadge><h2>No verified parlay qualifies</h2><p>The current provider does not supply a trusted executable combined quote. The optimizer will not invent payout terms or assume independence for correlated legs.</p><div className="parlay-rules"><span><Layers3 />Every leg must qualify alone</span><span><ShieldAlert />Separate 0.25%–0.75% sleeve</span><span><CircleCheckBig />PASS protects the bankroll</span></div></div></Panel>}<Panel title="Risk methodology" eyebrow="Cross-event parlay v1"><div className="method-grid"><div><strong>Joint probability</strong><p>Only estimated when correlation is defensible. Same-game parlays remain excluded.</p></div><div><strong>Incremental exposure</strong><p>Duplicate team, game, and market risk counts alongside existing straight positions.</p></div><div><strong>Conservative stake</strong><p>Parlays use a smaller Kelly fraction and separate daily sleeve cap than straight bets.</p></div></div></Panel></div>; }
